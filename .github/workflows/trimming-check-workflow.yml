name: Trimming Check
env:
  DOTNET_CLI_TELEMETRY_OPTOUT: 1
  DOTNET_NOLOGO: 1

on:
  push:
    branches:
      - main
    paths:
      - 'src/NuGetTrends.Web/**'
      - 'src/NuGetTrends.Web.Client/**'
      - 'src/NuGetTrends.Data/**'
      - 'src/Directory.Build.props'
      - '**/TrimmerRoots.xml'
      - '**/trimming-check-workflow.yml'
      - 'scripts/verify-trimming.cs'
  pull_request:
    paths:
      - 'src/NuGetTrends.Web/**'
      - 'src/NuGetTrends.Web.Client/**'
      - 'src/NuGetTrends.Data/**'
      - 'src/Directory.Build.props'
      - '**/TrimmerRoots.xml'
      - '**/trimming-check-workflow.yml'
      - 'scripts/verify-trimming.cs'
  workflow_dispatch:

jobs:
  trimming-check:
    name: IL Trimming Check
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Verify trimmed build renders correctly
        run: ./scripts/verify-trimming.cs
